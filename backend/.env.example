# Backend Environment Variables

# JWT Secret (MUST match frontend - minimum 32 characters)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-super-secret-key-min-32-chars-replace-this

# Neon PostgreSQL Database Connection (async driver with asyncpg)
# Format: postgresql+asyncpg://user:password@host/database?ssl=require
# Note: asyncpg uses 'ssl=require' instead of 'sslmode=require'
DATABASE_URL="postgresql+asyncpg://neondb_owner:npg_xd1zqHPKyni8@ep-ancient-dew-ah9ofl46-pooler.c-3.us-east-1.aws.neon.tech/neondb?ssl=require"

# CORS Origins (comma-separated for multiple origins)
CORS_ORIGINS=http://localhost:3000
